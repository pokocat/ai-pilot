# 依赖
node_modules/
**/node_modules/

# 构建产物
dist/
**/dist/
lib/
.temp/
.swc/
app/.taro-cache/

# 本地工具/评审产物
app/.impeccable/
*.tgz
*.zip
weapp-preview*.json
weapp-preview*.png
weapp-auto-preview*.json
weapp-auto-preview*.png

# IDE 云连接 / 技能下载 / 对话草稿
.cc-connect/
flova_skill_downloads/

# Claude Code 本地产物（worktree；settings.local.json 已由 *.local 覆盖；.claude/skills 仍入库）
.claude/worktrees/

# 微信开发者工具私有/误生成配置
/project.config.json
/project.private.config.json
app/project.private.config.json

# 根目录无依赖时 npm install 生成的空 lock
/package-lock.json

# 环境变量（含密钥，不入库；见各自 .env.example）
.env
server/.env
admin/.env
# 例外：server/.env.test 是测试库连接串（无真实密钥），纳入版本管理，保证 npm test 固定连 junshi_test。
!server/.env.test
*.local

# 日志
*.log
npm-debug.log*

# 测试覆盖率
coverage/

# 编辑器/系统
.DS_Store
.idea/
.vscode/
*.tsbuildinfo
.env
.preview/

# 密钥/凭证（如微信小程序上传私钥 private.*.key）——禁止入库
*.key
