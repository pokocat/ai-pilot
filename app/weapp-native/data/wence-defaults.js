// 问策入口终态的**本地兜底内容**（引导态文案，同 src/data/operatingSystem.ts 的定位）。
//
// 两条边界，别越：
// ① 这里只放「引导用户开口」的通用问法与游客开场白，**不得写入任何用户业务结论**——
//    真实主动消息由服务端从运营模板池（WenceTemplate kind='proactive'）生成并写进会话，
//    提示词池同理走 GET /wence/hints。本文件只在「游客（零服务端写入）」与「词池为空 / 拉取失败」时兜底。
// ② 对外内容归运营：任何时候运营录了模板，服务端下发的就必须盖过这里，端上不得反向优先本地池。
//
// 文案口径：克制、不促销、不解释权限（"不登录也能看"一类的话是明令禁止的，见 AGENTS §6）。

/**
 * 游客进场的本地主动消息（**只在内存里渲染，零服务端写入**）。
 * 游客没有档案、没有经营数据，真给不出「一个具体判断」；所以这里给的是
 * 「军师会怎么接这件事」+ 一个能立刻回答的问题，而不是"有什么可以帮你"式的空话。
 */
const GUEST_PRELUDE = [
  {
    text: '我是总军师。你不用先整理好再说——把眼下最拿不准的那件事直接讲出来，我先替你判断主要矛盾在哪，再决定要不要调专业军师。\n\n先说一句：现在最卡你的，是方向、是增长，还是钱和人？',
    chips: ['方向定不下来', '增长卡住了', '钱和人都紧'],
  },
];

/**
 * 提示问题 pill 的本地兜底词池（`GET /wence/hints` 空池或拉取失败时用）。
 * 都是老板一句话能接上的问法，不带行业假设、不带数字承诺。
 */
const FALLBACK_HINTS = [
  '这个月最该先干哪件事？',
  '增长卡在哪一环？',
  '这单要不要接？',
  '价格该不该动？',
  '手里的人该怎么排？',
  '上个月的账，问题出在哪？',
];

module.exports = { GUEST_PRELUDE, FALLBACK_HINTS };
