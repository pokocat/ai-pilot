# —— 军师后端环境变量（示例，复制为 .env） ——
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/junshi?schema=public"
PORT=4000

# AI 提供方：mock（默认，模板产出）| claude（真实模型）
AI_PROVIDER=mock
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-4-6

MODERATION_ENABLED=true
