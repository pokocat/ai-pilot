# 测试专用环境（P1-7）——纳入版本管理（无真实密钥，仅本地测试库连接串）。
#
# npm test / npm run pretest 通过 `node --env-file=.env.test` 在进程启动时先注入这些变量。
# 关键一：dotenv（env.ts）与 Prisma CLI 的 dotenv 都「不覆盖已存在的 process.env」，
# 因此即便 server/.env 的 DATABASE_URL 指向 dev 库（junshi），测试仍只连测试库（junshi_test），绝不误碰 dev 数据。
# 关键二（2026-07-27 补）：「不覆盖」不足以隔离——`.env` 里本文件**没声明**的键照样会被注入。
# 故 `src/env.ts` 在 NODE_ENV=test 时**整体跳过**加载 `server/.env`（守卫用例 test/envHermetic.test.ts）。
# 推论：测试要用的变量必须写进本文件，或在用例里显式 set/delete —— 不要指望开发机的 `.env`。
#
# 换机改这一行即可（本地测试库连接串，非密钥）。若你的本地库用户名不同，改这里。
DATABASE_URL="postgresql://donis@localhost:5432/junshi_test?schema=public"
NODE_ENV=test

# 快出片 BFF：默认不配置上游，单测显式注入假地址与 service token，绝不误连真实 aidrama。
AIDRAMA_CLIP_BASE_URL=""
AIDRAMA_CLIP_SERVICE_TOKEN=""
AIDRAMA_CLIP_ALLOW_PRIVATE_NET=false
AIDRAMA_CLIP_TIMEOUT_MS=1000

# 海报成品图（canvas_design）**不需要在这里配任何东西**：开关只有一层，即 DB 的 FeatureFlag
# 行 'creative-poster'（行缺失视为关）。用例自己 setFeatureFlag('creative-poster', true) 开启
# （记得随后 __clearFeatureCache()，那层读缓存 60s）。
# 曾有一行 CANVAS_DESIGN_ENABLED=true，配它是因为 env 是模块加载时冻结的单例、用例内改
# process.env 已经太晚 —— 那个部署级开关本身已删除，这个坑随之消失。
